# Sample scenario (committed)

Placeholder scaffolded at M0. This folder will hold **one tiny committed
scenario** plus **one sample output frame** so the repo is self-explanatory
without fetching large assets or running a full render.

Populated once the runner/verifier/render paths exist (M2–M4):

- `scenario.yaml` — a minimal scenario on a tiny scene (per brief §6.1).
- `sample_frame.png` — a single representative overlaid frame (small).

Everything under `outputs/` is gitignored; the one sample frame here is the
deliberate exception so reviewers can see the overlay style at a glance.
