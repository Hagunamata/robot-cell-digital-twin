"""Results catalog: sqlite schema + writer (one row per scenario run).

Placeholder package scaffolded at M0. Implementation lands in M3.
Same catalog spirit as Project 2. Schema is defined in §6.3 of the brief
(scenario_id, run_id, verdict, metrics, clip paths, asset_licenses, ...).
"""
