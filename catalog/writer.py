"""Catalog writer: persist one row per scenario run into sqlite.

Placeholder module (M0 scaffold — no logic yet). To be implemented in M3.
Applies catalog/schema.sql and writes verdicts + metrics + clip paths.
"""

from __future__ import annotations


def write_run(*args, **kwargs):
    """Insert one scenario-run row into the catalog. Not yet implemented."""
    raise NotImplementedError("catalog.writer.write_run is implemented in M3")
