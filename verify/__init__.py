"""Scenario verifier: reach / cycle-time / clearance checks -> verdicts.

Placeholder package scaffolded at M0. Implementation lands in M3:
consume a sim log, evaluate each criterion against config/safety.yaml and
the per-scenario overrides, and emit a verdict + metrics.

NOTE: safety thresholds are ILLUSTRATIVE for a portfolio demo. They are not
a claim of ISO 10218 / ISO/TS 15066 certified compliance.
"""
