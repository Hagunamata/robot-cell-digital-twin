# Python dependencies — M2 (headless MuJoCo sim + runner).
# Version floors kept conservative; tighten to exact pins once verified on the
# Ubuntu/WSL2 run target. Blender is NOT here — it runs natively on the host.

mujoco>=3.1,<4          # physics + headless offscreen rendering
numpy>=1.24
pyyaml>=6.0             # scenario / safety config

# Rendering/encoding: frames (M2), overlay drawing + MP4 encode (M4).
imageio>=2.31
imageio-ffmpeg>=0.4.9
pillow>=10.0            # lightweight text/shape overlay (in place of OpenCV)

# Dashboard (M6).
streamlit>=1.31        # reads the sqlite catalog; `streamlit run dashboard/app.py`

# Dev / smoke test.
pytest>=7.4

# Optional / added later:
#   lerobot[dataset]  # OPTIONAL (M5 DROID replay). Pulls torch + HF datasets —
#                  # install only when extracting a DROID episode:
#                  # `pip install 'lerobot[dataset]'` (the [dataset] extra is
#                  # required; plain `lerobot` omits HF `datasets`). Targets the
#                  # v3.0 dataset API (verified: lerobot 0.6.1). Once the episode
#                  # is cached (assets/droid_cache/), replay needs neither lerobot
#                  # nor network.
#   streamlit      # dashboard (M6, confirmed at M1)
