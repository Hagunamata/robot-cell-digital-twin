"""Blender (Cycles + OptiX) photoreal "hero" render — Windows host path.

Placeholder package scaffolded at M0. Implementation lands in M6.

IMPORTANT: these scripts run on the WINDOWS HOST natively to use the RTX
card. They are a deliberately separate, optional step — NOT part of the
WSL2/Docker container path. Do not assume GPU passthrough inside WSL2.
Only 2-3 chosen deck clips get the hero treatment.
"""
