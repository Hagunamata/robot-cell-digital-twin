# 03 — Finalization

Phase 3 of the `docs/` set. **Skeleton scaffolded at M0** — written at M7.

Planned contents:

- **Reflection** — what the scenario verifier does well, where the twin-style
  abstraction is honest about its limits, and what was intentionally left out.
- **Roadmap / future work:**
  - **Isaac Sim photoreal** simulation (once off the 8 GB laptop-GPU line).
  - **A live-sync true digital twin** — bidirectional telemetry and a closed loop
    to real hardware (the step beyond this offline verifier).
  - **Fleet monitoring** — the fourth project in the lifecycle arc
    (curate data → validate scenario → **monitor the fleet**).
- **Finalization checklist** — README asset + license table complete; no large
  binaries committed; `make demo` reproduces the v1 clips end-to-end.

> _To be written during M7._
